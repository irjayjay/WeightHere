import 'package:flutter/material.dart';

class ProjectColors {
  static const background = Color(0xFFF0F0F0);
  static const buttonPrimary = Colors.cyan;
  static const onButtonPrimary = Colors.white;
  static const text = Color(0xFF220000);
}
